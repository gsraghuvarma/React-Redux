// import { fromJS } from 'immutable';
// import { makeSelectAddstudentDomain } from '../selectors';

// const selector = makeSelectAddstudentDomain();

describe('makeSelectAddstudentDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
